{% extends 'master' %}

{% block header %}
  <h1>Uzdevums</h1>
  <p>Dzīvē pirmkodam ir sakrāla nozīme.</p>
{% endblock %}

{% block content %}
  {#  <p class="text-right editmenu">
    <a href="{{ 'problem/edit' | route({id: problem.id }) }}">
       <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Rediģēt
    </a>
  </p> #}
  <h2>
    {{ problem.name }}
  </h2>
  <hr>
  <div class="tex2jax_process">
    {{ problem.description | markdown | safe }}
  </div>
{% endblock %}
